
grand usage for all schemas in a  database :

do $$
DECLARE
   v_schema name;
BEGIN
   FOR v_schema IN
      SELECT nspname FROM pg_namespace
      WHERE nspname NOT IN ('pg_catalog', 'information_schema', 'pg_toast')
        AND nspname NOT LIKE 'pg_temp%'
        AND nspname NOT LIKE 'pg_toast_temp%'
   LOOP
      EXECUTE format(
                 'GRANT USAGE, CREATE ON SCHEMA %I TO readwrite',
                 v_schema
              );
   END LOOP;
end
$$;



grand SELECT, INSERT, UPDATE, DELETE on all tables in all schemas in a  database :

do $$
DECLARE
   v_schema name;
BEGIN
   FOR v_schema IN
      SELECT nspname FROM pg_namespace
      WHERE nspname NOT IN ('pg_catalog', 'information_schema', 'pg_toast')
        AND nspname NOT LIKE 'pg_temp%'
        AND nspname NOT LIKE 'pg_toast_temp%'
   LOOP
      EXECUTE format(
                 'GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA %I TO readwrite',
                 v_schema
              );
   END LOOP;
end
$$;

grand all previledges on all sequences in all schemas in a  database :


do $$
DECLARE
   v_schema name;
BEGIN
   FOR v_schema IN
      SELECT nspname FROM pg_namespace
      WHERE nspname NOT IN ('pg_catalog', 'information_schema', 'pg_toast')
        AND nspname NOT LIKE 'pg_temp%'
        AND nspname NOT LIKE 'pg_toast_temp%'
   LOOP
      EXECUTE format(
                 'GRANT ALL PRIVILEGES ON ALL SEQUENCES  IN SCHEMA %I TO readwrite',
                 v_schema
              );
   END LOOP;
end
$$;

grand select on all future tables in all schemas in a  database :

do $$
DECLARE
   v_schema name;
BEGIN
   FOR v_schema IN
      SELECT nspname FROM pg_namespace
      WHERE nspname NOT IN ('pg_catalog', 'information_schema', 'pg_toast')
        AND nspname NOT LIKE 'pg_temp%'
        AND nspname NOT LIKE 'pg_toast_temp%'
   LOOP
      EXECUTE format(
                 'ALTER DEFAULT PRIVILEGES FOR USER maxappuser_pep IN SCHEMA %I GRANT SELECT, INSERT, UPDATE, DELETE  ON TABLES TO readwrite',
                 v_schema
              );
   END LOOP;
end
$$;


grand select on all future sequence in all schemas in a  database :

"do $$
DECLARE
   v_schema name;
BEGIN
   FOR v_schema IN
      SELECT nspname FROM pg_namespace
      WHERE nspname NOT IN ('pg_catalog', 'information_schema', 'pg_toast')
        AND nspname NOT LIKE 'pg_temp%'
        AND nspname NOT LIKE 'pg_toast_temp%'
   LOOP
      EXECUTE format(
                 'ALTER DEFAULT PRIVILEGES FOR USER maxappuser_pep IN SCHEMA %I GRANT USAGE, SELECT, UPDATE ON SEQUENCES TO readwrite',
                 v_schema
              );
   END LOOP;
end
$$;"